export type Route = {
  // The pattern is the pathname of the route, should follow URLPattern syntax
  pattern: string;
  // The file is the path to the file that contains the route
  file: string;
};
